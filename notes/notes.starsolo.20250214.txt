== STARsolo output files for my runs

output dir:
s023> ls -F
Aligned.sortedByCoord.out.bam  Log.final.out  Log.out  Log.progress.out  Solo.out/

cd Solo.out
s023> ls -F
Barcodes.stats  GeneFull_Ex50pAS/

s023> cd GeneFull_Ex50pAS/
s023> ls -F 
CellReads.stats  Features.stats  Summary.csv  UMIperCellSorted.txt  filtered/  raw/

s023> ls -F raw
UniqueAndMult-PropUnique.mtx  barcodes.tsv  features.tsv  matrix.mtx

023> ls -F filtered/
barcodes.tsv  features.tsv  matrix.mtx


== STARsolo metrics
https://broadinstitute.github.io/warp/docs/Pipelines/Optimus_Pipeline/starsolo-metrics

Differences between GeneFull_Ex50pAS, GeneFull_ExonOverIntron, and GeneFull
https://github.com/alexdobin/STAR/issues/1460


== STARsolo soloCellReadStats file columns

url: https://github.com/alexdobin/STAR/issues/1501

question:

I understand that :
cbMatch        means the number of read that matched the cell barcode,
cbPerfect      for the number of perfect match on cell barcode
exonic         for the number of read mapping on exonic
intronic       for the number of read mapping on intronic
mito           for the number of read mapping on mitochondrial genome
nUMIunique     for the total number of counted UMI
nGenesUnique   for the number of genes having non 0 counts (not sure for this one)

answer:

your descriptions of the fields are all correct!
Other fields:
genomeU      : number of reads mapping to one locus in the genome
genomeM      : number of reads mapping to multiple loci in the genome
featureU     : number of reads mapping to one feature (Gene, GeneFull, etc)
featureM     : number of reads mapping to multiple features
cbMMunique   : number of reads with cell barcodes that map with mismatches to one barcode in the passlist
cbMMmultiple : number of reads with cell barcodes that map with mismatches to multiple barcodes in the passlist

exonicAS     : number of reads mapping antisense to annotated exons
intronicAS   : number of reads mapping antisense to annotated introns

These are only calculated for --soloFeatures GeneFull_Ex50pAS

exonic and intronic are only calculated for --soloFeatures GeneFull_Ex50pAS and/or GeneFull_ExonOverIntron

Multiple --soloFeatures options can be used at the same time, so if you want to use GeneFull for downstream analysis, but want to get separate intronic/exonic statistics, you can use --soloFeatures GeneFull GeneFull_Ex50pAS


question:

Thank you so much for your incredible work on this great tool! On the same topic could you please clarify the meaning of

countedU
countedM
nUMImulti
nGenesMulti


answer:

countedU : number of unique-gene reads that were used in counting UMIs (!= number of UMIs), i.e. reads with valid CB/UMI/gene
countedM : same for multi-gene reads
nUMImulti : number of UMI for multi-gene reads, if requested
nGenesMulti : number of genes supported by just multi-gene reads, if requested


== Additional needed files by assign_hash.R

    assign_hash.R \
        $key \
        $hash_mtx \
        $hash_cell \
        $hash_hash \
        ${cds_dir}/*cds.RDS \
        $umis_per_cell \
        $params.hash_umi_cutoff \
        $params.hash_ratio

   Need: umis per cell barcode

        set key, file("*UMIs.per.cell.barcode.txt") into for_assign_hash_umis

UMIs.per.cell.barcode.txt is made in process count_umis_by_sample in bbi-sci/main.nf



== bbi-sci colData()

> cds <- readRDS('SeahubZ01_cds.RDS')
> colnames(colData(cds))
"cell"
"Size_Factor"
"n.umi"
"perc_mitochondrial_umis"
"emptyDrops_FDR"
"emptyDrops_Limited"     
"intron_fraction"
"scrublet_score"         

> cds <- readRDS('SeahubZ01_hash_cds.RDS')
> colnames(colData(cds))
"cell"
"Size_Factor"
"n.umi"
"perc_mitochondrial_umis"
"emptyDrops_FDR"
"emptyDrops_Limited"      
"intron_fraction"
"scrublet_score"
"P5_barcode"
"P7_barcode"
"RT_barcode"
"Ligation_barcode"
"RT_plate"
"hash_umis"
"pval"
"qval"
"top_to_second_best_ratio"
"top_oligo"          


== STARsolo CellReads.stats definitions

URL: https://github.com/alexdobin/STAR/discussions/1826

cbMatch       means the number of read that matched the cell barcode,
cbPerfect     for the number of perfect match on cell barcode
cbMMunique :  number of reads with cell barcodes that map with mismatches to one barcode in the passlist
cbMMmultiple: number of reads with cell barcodes that map with mismatches to multiple barcodes in the passlist

genomeU:      number of reads mapping to one locus in the genome
genomeM:      number of reads mapping to multiple loci in the genome
featureU:     number of reads mapping to one feature (Gene, GeneFull, etc)
featureM:     number of reads mapping to multiple features

exonic:       number of reads mapping to annotated exons
intronic:     number of reads mapping to annotated introns
These are only calculated for --soloFeatures GeneFull_Ex50pAS and/or GeneFull_ExonOverIntron

exonicAS:     number of reads mapping antisense to annotated exons
intronicAS:   number of reads mapping antisense to annotated introns
These are only calculated for --soloFeatures GeneFull_Ex50pAS

countedU:     number of unique-gene reads whose UMIs contributed to counts in the matrix.mtx
countedM:     number of multi-gene reads whose UMIs contributed to counts in the matrix.mtx

mito:         for the number of read mapping on mitochondrial genome

nUMIunique:   for the total number of counted UMI for unique-gene reads
nGenesUnique: for the number of genes for unique-gene reads
nUMImulti:    for the total number of counted UMI for multi-gene reads
nGenesMulti:  for the number of genes for multi-gene reads


== ScaleRna STARsolo parameters

STAR --runThreadN 14
     --genomeDir zebrafish_star_2_7
     --soloType CB_UMI_Simple
     --soloBarcodeReadLength 0
     --soloCBwhitelist None
     --soloCBtype String
     --outSAMtype None
     --outSJtype None
     --soloCellReadStats Standard
     --soloStrand Forward
     --soloFeatures GeneFull_Ex50pAS
     --soloMultiMappers PropUnique
     --readFilesIn demuxed_ubam1.bam,demuxed_ubam2.bam,demuxed_ubam3.bam,demuxed_ubam4.bam,demuxed_ubam5.bam,demuxed_ubam6.bam,demuxed_ubam7.bam,demuxed_ubam8.bam,demuxed_ubam9.bam,demuxed_ubam10.bam,demuxed_ubam11.bam,demuxed_ubam12.bam,demuxed_ubam13.bam,demuxed_ubam14.bam,demuxed_ubam15.bam,demuxed_ubam16.bam,demuxed_ubam17.bam,demuxed_ubam18.bam,demuxed_ubam19.bam,demuxed_ubam20.bam,demuxed_ubam21.bam,demuxed_ubam22.bam,demuxed_ubam23.bam,demuxed_ubam24.bam,demuxed_ubam25.bam,demuxed_ubam26.bam,demuxed_ubam27.bam,demuxed_ubam28.bam,demuxed_ubam29.bam,demuxed_ubam30.bam,demuxed_ubam31.bam,demuxed_ubam32.bam
     --readFilesType SAM SE
     --readFilesCommand samtools view
     --soloInputSAMattrBarcodeSeq CB UM
     --soloCellFilter None
     --outFilterMultimapNmax 6


